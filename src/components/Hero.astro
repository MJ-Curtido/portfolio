<section id="hero">
    <div id="hero-content">
        <div id="hero-introduction">
            <p id="hero-greeting">Hola, soy</p>
            <h1 id="hero-title">Manuel Jesús Curtido Rosado</h1>
            <h2 id="hero-subtitle">Desarrollador Full-Stack | React · Next.js</h2>

            <p id="hero-description">
                Con 1,5 años de experiencia profesional construyendo y refactorizando aplicaciones web eficientes con React, Next.js y Redux Saga en
                JavaScript. Me enfocaba en la optimización continua del código y en el mantenimiento evolutivo según los requerimientos del cliente.
                Actualmente, continúo expandiendo mis competencias en el lado del servidor especializándome en Node.js, al mismo tiempo que exploro la
                integración de Inteligencia Artificial en el desarrollo de software y la automatización de flujos de trabajo modernos con Docker.
            </p>
        </div>

        <div id="hero-visual">
            <div id="visual-inner">
                <span>&lt; / &gt;</span>
            </div>
        </div>
    </div>

    <section id="hero-action">
        <a
            href="https://www.linkedin.com/in/manuel-jes%C3%BAs-curtido-rosado-2337a726a"
            target="_blank"
            rel="noopener noreferrer"
            class="hero-buttons">LinkedIn</a
        >
        <a href="https://github.com/MJ-Curtido" target="_blank" rel="noopener noreferrer" class="hero-buttons">GitHub</a>
        <a href="mailto:manu17pc@gmail.com" class="hero-buttons">Email</a>
        <a href="tel:+34610925034" class="hero-buttons">Teléfono</a>
    </section>
</section>

<style>
    #hero {
        margin: 0 auto;
        max-width: var(--max-width-pc);
        padding: 4rem var(--page-padding);
    }

    #hero-content {
        align-items: center;
        display: grid;
        gap: 2rem;
        grid-template-columns: 1.2fr 0.8fr;
    }

    #hero-introduction {
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    #hero-greeting {
        color: var(--text);
        font-size: 1.2rem;
        font-weight: 500;
        margin-bottom: 0.5rem;
        opacity: 0.8;
    }

    #hero-title {
        color: var(--text);
        font-size: 3.7rem;
        margin-bottom: 1rem;
    }

    #hero-subtitle {
        color: var(--highlight);
        font-weight: 600;
        margin-bottom: 1.5rem;
    }

    #hero-description {
        text-align: left;
        color: var(--text);
        font-size: 1.1rem;
        line-height: 1.6;
        opacity: 0.85;
    }

    #hero-action {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
        justify-content: center;
        max-width: 1200px;
        margin: 3rem auto 0 auto;
    }

    .hero-buttons {
        align-items: center;
        background: transparent;
        border-radius: 20px;
        border: 2px solid var(--border-accent);
        color: var(--text);
        display: inline-flex;
        font-size: 1rem;
        font-weight: 600;
        justify-content: center;
        padding: 0.8rem 2rem;
        text-decoration: none;
        transition: all 0.2s ease;
    }

    .hero-buttons:hover {
        border-color: var(--text);
        transform: translateY(-2px);
    }

    #hero-visual {
        align-items: center;
        display: flex;
        justify-content: center;
    }

    /* TODO: Add photo and remove every unnecesary code */
    #visual-inner {
        align-items: center;
        backdrop-filter: blur(5px);
        background: rgba(125, 125, 125, 0.03);
        border-radius: 24px;
        border: 1px solid var(--border-accent);
        color: rgba(125, 125, 125, 0.3);
        display: flex;
        font-size: 3rem;
        font-weight: bold;
        height: 250px;
        justify-content: center;
        width: 250px;
    }
</style>
